apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  selector:
    app: apigateway
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }} 
      targetPort: {{ .Values.service.targetPort }}
