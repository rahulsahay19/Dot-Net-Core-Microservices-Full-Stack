apiVersion: v1
kind: Service
metadata:
  name: basketapi
  labels:
    app: basketapi
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    app: basketapi
